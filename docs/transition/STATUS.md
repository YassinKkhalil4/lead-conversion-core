# Transition Status

Last updated: 2026-08-09

| Mini-project | Status | Verification level | Notes |
|---|---|---|---|
| MP-01 Baseline, security, source cleanup | locally_verified | local file inspection, tracked-artifact scan, and source tests | Clean repo created from canonical source. Secret-bearing archive artifacts excluded. Current behavior fixtures added. `npm run artifacts:scan` verifies ignored runtime archives and credential-like files are not tracked. |
| MP-02 Build, deployment, migrations, recovery | locally_verified | local npm, PostgreSQL, backup/restore tests, static Compose config, shell syntax checks | Lockfile, fixed scripts, single migrator, checksums, advisory lock, heartbeat, readiness, backup/restore verified locally. The migrator rejects all operator arguments before opening PostgreSQL. Backup/restore scripts avoid password-bearing database URL command arguments by using private libpq service files; backup, restore, and restore verification reject positional/flag arguments before reading required secret-bearing env; backup output paths are locked per timestamp, existing encrypted dump/checksum paths are refused, incomplete encrypted output/checksum files are cleaned up on failure, and restore verifies the encrypted dump SHA-256 checksum by default before decryption. `.env.example` matches validated env flags, generated env files rotate local placeholders without passing generated secrets as child-process arguments, enabled external integration flags require their corresponding credentials or target secrets at startup, Compose defines both legacy outbox and durable runtime worker containers, API and worker startup do not run migrations or configuration seeding, production build output excludes compiled tests while `npm run lint` still typechecks tests, `/ready` requires enabled workers to report fresh operational heartbeat metadata, and PostgreSQL 16 dump inspect/restore-smoke wrappers are present. Docker daemon unavailable for image run or dump command execution. |
| MP-03 PostgreSQL core and Airtable migration | locally_verified | local PostgreSQL integration tested for schema, importer, reconciliation foundations, and CLI parser hardening | Target schemas/tables added. Importer supports JSON/CSV dry-run, manifest awareness, stable ID enforcement, phone/email collision reporting, duplicate/reject reporting, relationship rejection, raw records, clients, projects, salespeople, contacts, contact opt-out preservation, leads, qualifications, scores, messages, follow-ups, appointments, and historical events into append-only audit records. Airtable import and reconciliation CLIs reject unknown, duplicate, malformed, unsafe, or ambiguous operator arguments before loading source files or querying PostgreSQL. Reconciliation blocks on rejected records, maps accepted source rows separately from rejected source rows, and records accepted-row business checks for lead status distribution, active leads, stop-follow-up, opt-out, pending follow-ups, open/booked appointments, and message provider-ID uniqueness. Complete Airtable export still pending for production reconciliation. |
| MP-04 Durable inbox, outbox, jobs, audit | locally_verified | local PostgreSQL integration tested for dedupe, leases, retry, dead-lettering, atomicity, jobs, worker orchestration, replay, and audit append-only behavior | New durable runtime foundation added alongside legacy `edge_outbox` compatibility path. Runtime retries use bounded exponential backoff with jitter unless a capped provider retry hint is supplied, while malformed scheduled-job payloads can dead-letter immediately with attempt history. Runtime worker is disabled unless configured and only claims work when real handlers are provided. Legacy `edge_outbox` compatibility retries now have a terminal `dead_lettered` state. |
| MP-05 WhatsApp messaging platform | staging_blocked | local contract fixtures and PostgreSQL/API integration tests | Provider-neutral messaging port, disabled Meta WhatsApp adapter, durable outbox dispatcher mapping, internal WhatsApp message request API/service, template allow-list validation, 24-hour session-window policy, Meta status webhook durable inbox ingestion/processing, monotonic delivery-state advancement for out-of-order provider statuses, and n8n compatibility routes added. n8n-compatible WhatsApp status callbacks now durably receipt before relationship resolution. Live verification is blocked on owner Meta setup. |
| MP-06 Versioned configuration | staging_blocked | local PostgreSQL integration, invalid fixtures, CLI parser coverage, and seed/export parity tests | Immutable published configuration versions, active pointers, validation/diff/publish/active/rollback service and CLI foundation added while preserving legacy `edge_config_snapshots` compatibility. Runtime config reads prefer `configuration.active_versions` with legacy fallback; invalid configs are rejected; legacy conversations pin immutable `configuration.versions` UUIDs; `npm run seed` now publishes through versioned configuration authority, idempotently maintains the legacy compatibility snapshot, and rejects all operator arguments before PostgreSQL access; `npm run config` rejects ambiguous, command-inappropriate, duplicate, empty, or control-character-bearing operator arguments before service construction; legacy `/internal/config/import`, `/internal/config/sync`, and `npm run sync-config` mutation paths are disabled by default behind explicit compatibility flags, and live Airtable sync additionally requires `AIRTABLE_TOKEN`; Airtable config exports compile to the same deterministic version as the seed source. Real-export reconciliation remains pending owner action. |
| MP-07 Lead intake and CRM | staging_blocked | local PostgreSQL/API integration tested for internal/source ingress and projection hook | Authenticated internal lead intake validates source payloads, upserts contacts/leads idempotently, matches projects without inventing relationships, records audit metadata, suppresses first contact for opted-out contacts, enqueues first-contact WhatsApp outbox commands, processes durably received website/Facebook source webhooks through the runtime inbox worker, and enqueues Airtable read-only visibility projection commands. Live verification is blocked on owner source/Airtable setup. |
| MP-08 Conversation and qualification | staging_blocked | local PostgreSQL/API integration tested for durable Meta and n8n inbound processing | Signed Meta and authenticated n8n-compatible inbound message paths durably receipt into `runtime.inbox_events`; unsupported signed Meta webhooks are durably receipted and then marked ignored by the runtime worker instead of remaining pending. n8n-compatible inbound callbacks acknowledge durable receipt before client relationship resolution, leaving missing-channel and missing-client outcomes to runtime workers. Runtime workers process only edge-owned conversations into `edge_conversations`, `app.conversations`, `app.messages`, `app.qualification_answers`, audit events, and transactional WhatsApp outbox commands. Legacy-owned conversations remain ignored for Typebot fallback. Explicit opt-out and human-takeover suppression persist control state and avoid outbound side effects. English/Arabic final qualification completion persists completed sessions and qualified lead state. Live staged turn verification remains blocked on owner Meta/n8n setup. |
| MP-09 Scoring, routing, commands, alerts | staging_blocked | local domain, PostgreSQL/API, and dispatcher contract tests | Deterministic `real_estate_v1` scoring/routing persists score and routing runs, assigns same-client/project salespeople with stable tie-breaks, enqueues and dispatches durable assignment/no-eligible notification commands through the real Meta adapter path, and processes authenticated salesperson commands for acknowledgement, takeover, close-lost, and stop-follow-up. Live verification remains blocked on Meta/n8n setup, approved notification copy/templates, and real salesperson/project export parity. |
| MP-10 Follow-ups, SLA, reporting | staging_blocked | local PostgreSQL integration tests for durable follow-up, SLA, and reporting scheduling/execution | Follow-up scheduling/execution persists semantic `app.followups` rows and `runtime.scheduled_jobs` with explicit timezone, duplicate prevention, cancellation, expired-lease recovery, atomic outbound message/outbox enqueue, and audit. SLA scheduling/execution persists semantic `app.sla_jobs`, cancels obsolete reminders/escalations on acknowledgement/close/takeover/opt-out, executes due reminders/escalations into durable outbox commands, and recovers expired job leases. Daily reporting persists semantic `app.daily_reports`, generates SQL-backed summaries, recovers expired leases, enqueues operator report outbox commands idempotently, and materializes the next durable daily occurrence after a successful send while preserving the client-local clock across DST. Live report recipient/template verification remains blocked on owner inputs. |
| MP-11 Appointments and Calendar | staging_blocked | local PostgreSQL integration and calendar dispatcher/reconciliation contract tests | Appointment offers and slots persist semantic identities, booking uses PostgreSQL locks and idempotency keys, duplicate replies collapse to one appointment, cancelled offers cannot book, and booked appointments enqueue durable `calendar.create_event` outbox commands. Google Calendar adapter shape is present and disabled unless real credentials are configured; free/busy availability and pre-create recheck are implemented with retry/busy/network classification and bounded numeric/date retry-hint parsing. Delivered calendar create commands persist provider event IDs back to `app.appointments`; create-event network failures and delivery-unknown outcomes preserve replay safety without blind duplicate event creation; `npm run calendar:reconcile` exposes operator-visible local reconciliation for ambiguous creates and now rejects unknown, duplicate, malformed, unsafe, or non-canonical operator arguments before querying PostgreSQL. Live Google verification remains blocked on owner credentials/calendar IDs. |
| MP-12 Direct ingress, rollout, decommission | staging_blocked | local route-gating, deployment-script, cutover-readiness, and decommission-readiness tests | Direct Meta webhook and direct website/Facebook lead ingress routes are disabled by default and require explicit `DIRECT_META_WEBHOOK_ENABLED=true` or `DIRECT_LEAD_INGRESS_ENABLED=true`; startup validation rejects direct route flags and enabled n8n compatibility routes unless the required runtime worker processing flags are also enabled. Disabled direct-ingress route checks return unavailable before provider token, signature, or edge shared-secret validation, so disabled staging verification is not blocked on missing rotated credentials when shadow verification is skipped. Lead routes acknowledge only durable inbox receipt while the runtime worker owns validation/intake processing. Legacy synchronous `/v1/turn` active compatibility is disabled by default behind `ACTIVE_TURN_COMPAT_ENABLED`. n8n compatibility routes remain separately gated for fallback and now durably receipt callback events before client relationship resolution; the runtime worker wires those n8n WhatsApp/status/command processors whenever n8n compatibility routes are enabled, even if direct Meta webhook processing is disabled. `WORKER_KIND` is a closed deployment role (`outbox` or `runtime`) so typoed worker roles fail startup validation before selecting the wrong worker loop. `npm run cutover:readiness` reports direct-route flags, requires at least one direct-ingress route family to be selected, verifies required direct-inbox processor metadata including ignored Meta webhook receipts, verifies n8n compatibility inbox processor metadata when fallback is enabled, reports legacy active-turn compatibility state, fallback config, inbox/outbox/due scheduled-job backlog, delivery-unknowns, terminal outbox failures, dead letters, and runtime heartbeat age/operational metadata without mutating queues. Cutover/decommission readiness CLIs reject unknown or malformed operator arguments, and threshold arguments accept only canonical base-10 non-negative safe-integer strings, before querying PostgreSQL. `scripts/verify-deployment.sh` can verify direct Meta challenge and POST enabled/disabled behavior, including signed non-customer webhook receipt, unsigned webhook rejection, disabled POST unavailability, direct website/Facebook lead ingress enabled/disabled behavior, and n8n compatibility fallback enabled/disabled behavior against a supplied staging base URL without sending real customer messages; enabled direct-lead and enabled n8n fallback verification expect durable receipt acknowledgement from non-business probes, avoid creating authoritative lead/customer-message state, keep verifier secrets out of curl command arguments and child-process environments, load one-line env values through a parser instead of `source`, reject duplicate env keys and decoded NUL/newline values, reject duplicate, empty, unknown, or control-character-bearing verifier arguments before env loading, chmod parsed env assignment temp files private before writing values, and use run-scoped durable probe IDs so quick reruns cannot collide with prior inbox receipts. Disabled n8n fallback verification still requires `EDGE_INTERNAL_SECRET` because it verifies an internal authenticated route. `scripts/shadow-sequence.sh` also keeps the shared secret out of curl arguments, rejects CLI arguments before reading `.env`, loads `.env` without shell execution, duplicate-key overrides, or decoded NUL/newline values, chmods parsed env assignment temp files private, and uses run-scoped event IDs. `npm run decommission:readiness` reports n8n, Typebot, and Airtable projection exit criteria, including `active_turn_compat_disabled` and `n8n_compatibility_routes_disabled`; recent legacy-owned conversation activity, active legacy-only config snapshots, draft active configuration pointers, imported-only qualification volume, n8n semantic scheduled-job identities, and missing website or Facebook direct-lead stability evidence block fallback removal; direct-ingress stability requires current direct-ingress runtime authority, fresh matching runtime-worker heartbeat metadata, and aged completed-processing business-ingress evidence for every currently enabled direct route family, and direct lead ingress requires both website and Facebook processed evidence; it does not count ignored deployment probes or provider delivery-status callbacks. Enabled n8n compatibility routes, parked or dead-lettered legacy edge outbox rows, dead-lettered n8n inbox events, rejected n8n salesperson commands, cancelled Airtable projection commands, missing required Airtable reconciliation check evidence, and any non-pass Airtable reconciliation result block fallback removal until operator-resolved. Caddy/DNS changes, production cutover, live external verification, and legacy decommission remain owner-action blocked. |

Allowed statuses: `not_started`, `investigating`, `implementing`, `locally_verified`, `staging_blocked`, `staging_verified`, `production_canary`, `completed`.
